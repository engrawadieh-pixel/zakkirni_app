name: zakkirni_app
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.19.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_local_notifications: ^17.1.2
  flutter_native_timezone: ^2.0.0   # ✅ النسخة الصحيحة لإدارة التايم زون

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^17.1.2
  timezone: ^0.9.2

  
